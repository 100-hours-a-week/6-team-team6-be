spring:
  config:
    activate:
      on-profile: prod

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}

---
spring:
  config:
    activate:
      on-profile: dev

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}

---
spring:
  config:
    activate:
      on-profile: local

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
